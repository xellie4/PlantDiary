<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_plantdiary"
    app:title="@string/app_name"
    tools:context=".RegisterActivity">

    <ImageView
        android:id="@+id/quizzerlogo"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="100dp"
        android:layout_marginEnd="10dp"
        android:visibility="visible"
        app:srcCompat="@drawable/lasttry_png1"
        tools:visibility="visible" />

    <EditText
        android:id="@+id/username_editText"
        android:layout_width="320dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/quizzerlogo"
        android:layout_marginTop="30dp"
        android:background="#32000000"
        android:drawableStart="@drawable/ic_action_user"
        android:drawableLeft="@drawable/ic_action_user"
        android:ems="15"
        android:gravity="center"
        android:hint="Email Address"
        android:fontFamily="@font/proximnovaaltlight"
        android:importantForAutofill="no"
        android:inputType="textPersonName" />

    <EditText
        android:id="@+id/password_editText"
        android:layout_width="320dp"
        android:layout_height="40dp"
        android:layout_below="@+id/username_editText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:autofillHints=""
        android:background="#32000000"
        android:drawableStart="@drawable/ic_action_lock"
        android:drawableLeft="@drawable/ic_action_lock"
        android:ems="15"
        android:gravity="center"
        android:fontFamily="@font/proximnovaaltlight"
        android:hint="password"
        android:inputType="textPassword" />

    <Button
        android:id="@+id/registerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/password_editText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="45dp"
        android:background="@drawable/custom_button2"
        android:elevation="4dp"
        android:text="register"
        android:textColor="#111"
        android:textSize="13sp" />


    <Button
        android:id="@+id/gotologinbutton"
        android:layout_width="250dp"
        android:layout_height="25dp"
        android:layout_below="@+id/registerButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="57dp"
        android:background="@null"
        android:fontFamily="@font/robotomedium"
        android:text="Already an user? Log in now"
        android:textColor="@color/teal3" />

</RelativeLayout>
